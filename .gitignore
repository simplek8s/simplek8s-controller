# Binaries: ./bin = Makefile tooling, ./build = build artifacts
/bin/
/build/
/dist/
*.exe
/simplek8s-controller

# Go
vendor/
coverage.out
*.test

# nodectl embedded keyring copy (build-time from keys/, see
# cmd/nodectl/keyring.go; never commit the copy)
/cmd/nodectl/pubring.gpg

# IDE / OS
.idea/
.vscode/
*.swp
.DS_Store

# Secrets / local configs
*.local.yaml
kubeconfig
